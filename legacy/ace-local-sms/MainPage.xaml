<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SmsSenderApp.MainPage" >

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Image
                Source="dotnet_bot.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="dot net bot in a race car number eight" />

            <Label
                Text="ACE TAXIS SMS SENDER"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label
                x:Name="CounterLabel"
                Text="PROCESSED: 0"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="elcome to dot net Multi platform App U I" />
            <Label
                x:Name="TickLabel"
                Text="0"
                Style="{StaticResource SubHeadline}"/>
            <Label
                x:Name="SentLabel"
                Text="-"
                Style="{StaticResource SmallHeadline}"/>
            <Button
                x:Name="CounterBtn"
                Text="START SENDER" 
                Clicked="OnStartStopClicked"
                HorizontalOptions="Fill" />
            <Label x:Name="brightnessLabel" />
            <Slider x:Name="brightnessSlider" ValueChanged="Slider_ValueChanged" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
